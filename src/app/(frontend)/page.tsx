import React from 'react';
import api from '../../api';
import { redirect } from 'next/navigation';
export default async function HomePage() {
  return (
    <div>
      <h1>главная</h1>
    </div>
  );
}
